// Find ascii value of given number

#include <stdio.h>
int main()
{
    int a;
    printf("Enter a ascii value: ");
    scanf("%d", &a);

    printf("ASCII value of %d = %c", a, a);
    return 0;
}